{
  "name": "<%= name %>",
  "version": "1.0.0",
  "description": "<%= description || 'MCP Server generated by create-mcp-server' %>",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "<%= name %>": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
<% if (includeTests) { %>
    "test": "node --test dist/__tests__/",
<% } %>
    "inspector": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"<% if (auth && auth.type === 'oauth') { %>,
    "jsonwebtoken": "^9.0.0"<% } %>
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
